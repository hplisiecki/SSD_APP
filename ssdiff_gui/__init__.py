"""SSD - Desktop application for Supervised Semantic Differential analysis."""

__version__ = "1.0.0"
__author__ = "SSD Team"

# "standard" — European languages only (smaller build)
# "full"     — all languages including Asian (zh, ja, ko) which need
#              extra tokenizer backends (spacy-pkuseg, sudachipy, etc.)
EDITION = "standard"
